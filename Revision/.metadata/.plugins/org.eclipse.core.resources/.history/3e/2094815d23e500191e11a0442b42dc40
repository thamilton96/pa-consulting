package revision;

import java.util.Scanner;
import java.util.Stack;

public class Brackets {
	public static String balancedBrackets(String pattern) {
		
		String str = "";
		int openCurly;
		int closedCurly;
		int openBrace;
		int closedBrace;
		int openSquare;
		int closedSquare;
		
		//check if string is even or odd length, if odd length ignore
		if(String.length() == 0 || String.length() % 2== 1) {
			result = false;
		}
		
		//check and count types of bracket in string
		if(int openCurly == "{" ) {
			return openCurly++
		}
		
		if(int closedCurly == "}" ) {
			return 
		}
			
		if(int openBrace == "(") {
			
		}
		
		if(int closedBrace == ")") {
			
		}
		
		if(int openSquare == "[") {
			
		}
		
		if(int closedSquare == "]") {
			
		}
		
		
		for(int index =0; index < string.length(); index++ ) {
			
		}
		
		
		//after we have checked that string contains braces we need to see if there is an even amount of them
		if (openCurly == closedCurly && openBrace == closedBrace
	
	
    }
}
	
		