package revision;

import java.util.Scanner;
import java.util.Stack;

public class Brackets {

	public static String balancedBrackets(String pattern) {

		String result = "Balanced";

		String str = "";
		int openCurly;
		int closedCurly;
		int openBrace;
		int closedBrace;
		int openSquare;
		int closedSquare;

		//check if string is even or odd length, if odd length ignore
		if(pattern.length() == 0 || pattern.length() % 2 == 1) {
			result = "Not balanced";
		}

		//for loop checks and counts types of bracket in string
		for(int index =0; index < pattern.length(); index++ ) {
			System.out.println(index);

		//checking character at each index of string	
		char braces = pattern.charAt(0);
		System.out.println(braces);

		}

		if(int openCurly == "{" ) {
			return openCurly++;
		}
		
		//
		//		if(int closedCurly == "}" ) {
		//			return 
		//		}
		//
		//		if(int openBrace == "(") {
		//
		//		}
		//
		//		if(int closedBrace == ")") {
		//
		//		}
		//
		//		if(int openSquare == "[") {
		//
		//		}
		//
		//		if(int closedSquare == "]") {
		//
		//		}
		//
		//		}

		//after we have checked that string contains braces we need to see if there is an even amount of them
		//		if (openCurly == closedCurly && openBrace == closedBrace


		return result;
	}
}